{
  "name": "satvantage-copiloto-extension",
  "version": "0.5.0",
  "private": true,
  "description": "SatVantage Copiloto — extensão Chrome (Side Panel)",
  "scripts": {
    "build": "node build.js",
    "pack": "node build.js"
  }
}
